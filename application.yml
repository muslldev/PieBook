spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/piebook_db
    username: baker
    password: baker_classic55
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
